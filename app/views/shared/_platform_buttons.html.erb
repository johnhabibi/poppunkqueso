<% stacked = local_assigns.fetch(:stacked, false) %>
<% classes = stacked ? "flex flex-col gap-3 sm:flex-row" : "flex flex-wrap gap-3" %>
<div class="<%= classes %>">
  <%= link_to platform_urls[:spotify], class: platform_link_classes(primary: true), target: "_blank", rel: "noopener", data: { analytics_event: "click_spotify" } do %>
    Spotify
  <% end %>

  <%= link_to platform_urls[:apple_music], class: platform_link_classes, target: "_blank", rel: "noopener", data: { analytics_event: "click_apple_music" } do %>
    Apple Music
  <% end %>

</div>
